'use client'

// This is just a wrapper to reuse the existing analytics page component
import AnalyticsPage from '../analytics/page'

export default function AnalyticsTab() {
  return <AnalyticsPage />
}